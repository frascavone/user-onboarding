jobs:
  build:
    runs-on: pop-os
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 18
        - run: npm ci
        - run: npm run test
        - run: npm run build
        - run: npm run deploy

